# =============================================================================
# Cybersecurity Newsletter Editor Agent — Environment Variables
# =============================================================================

# Required: OpenRouter API Key
# Get your free key at https://openrouter.ai/
# Free models are available for testing (e.g., openai/gpt-oss-120b)
OPENROUTER_API_KEY=sk-or-v1-<your-api-key>

# =============================================================================
# Optional: Production Infrastructure
# =============================================================================

# PostgreSQL Storage (default: in-memory, non-persistent)
# STORAGE_TYPE=postgres
# DATABASE_URL=postgresql+asyncpg://<user>:<password>@<host>:<port>/<database>?ssl=require

# Redis Scheduler (default: in-memory, single-process)
# SCHEDULER_TYPE=redis
# REDIS_URL=rediss://default:<password>@<host>:<port>

# Sentry Error Tracking
# SENTRY_ENABLED=true
# SENTRY_DSN=https://<key>@<org-id>.ingest.sentry.io/<project-id>
